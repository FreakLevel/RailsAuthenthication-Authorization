<h1>Reminders#index</h1>
<p>Find me in app/views/reminders/index.html.erb</p>
<%= link_to 'New', reminders_new_path %>
<%= form_tag(destroy_user_session_path, method: 'delete') do %>
  <%= submit_tag('Log out') %>
<% end %>
<%= @reminders.each do |reminder| %>
  <%= reminder.title %>
  <%= reminder.message %>
  <%= reminder.datetime %>
  <%= link_to 'Delete', reminders_delete_path(id: reminder.id), method: 'delete' %>
<%
=begin %>
          <%= form_tag(reminders_delete_path, method: 'delete') do %>
    <%= submit_tag('Delete') %>
  <% end %>
  <%
=end
%>

<% end %>
